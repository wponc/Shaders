{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "dat.gui": "^0.7.9",
    "gh-pages": "^5.0.0",
    "three": "^0.150.1"
  }
}
